---
const { lists } = Astro.props;
interface ListItem {
  path: string;
  name: string;
}
---
{ Astro.url.pathname !== import.meta.env.BASE_URL && // index以外
<aside id="pankuzu">
  <ul class="">
    <li><a href="/sumotsu_a/top/">top</a></li>
    {
      lists.map((list: ListItem, index: number) => (
       list.name  !== "top" && ( // top以外
        <li>
          {lists.length - 1 !== index ? (
            <a href={`/sumotsu_a/${list.path}`}>{list.name}</a>
            ) : (
            <span>{list.name}</span>
            )}
        </li>
       )
      ))
    }
  </ul>
</aside>
}
