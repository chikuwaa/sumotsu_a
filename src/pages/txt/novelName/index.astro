---
import BaseLayout from '../../../layouts/BaseLayout.astro';

import NovelTitle from '../../../components/NovelTitle.astro';
import NameChange from '../../../components/NameChange.astro';

const pageTitle = "シリーズ名タイトル";
const pankuzuLists =[
  {name: "txt" ,path:"txt/"},
  {name: pageTitle }
]

const allPosts = await Astro.glob('./*.md');

---
<BaseLayout pageTitle={pageTitle} pankuzu ={pankuzuLists}>
  <main id="novelList">
    <h2 class="tit_middle">[{pageTitle}]</h2>
    <NameChange defaultFamName="鈴木" defaultName="花子" classFamName="charmname1" className="charmname3" />
    <ul class="novelLinkList">
      {allPosts.map((post) => <NovelTitle tit={post.frontmatter.title} url={post.url} desc={post.frontmatter.description} ura={post.frontmatter.ura}/>)}
    </ul>
  </main>
</BaseLayout>